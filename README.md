# Anomaly Detection in time series
Comparison of classical and representation-based methods for anomaly detection in time series.

## Setup
```bash
# Create a virtual environment
uv sync

# Download dataset
uv run scripts/download_data.py
```
